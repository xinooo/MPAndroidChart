package com.example.MPAndroidChart.greenDAO.gen;

import java.util.Map;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.AbstractDaoSession;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.identityscope.IdentityScopeType;
import org.greenrobot.greendao.internal.DaoConfig;

import com.example.MPAndroidChart.greenDAO.Ledger;

import com.example.MPAndroidChart.greenDAO.gen.LedgerDao;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * {@inheritDoc}
 * 
 * @see org.greenrobot.greendao.AbstractDaoSession
 */
public class DaoSession extends AbstractDaoSession {

    private final DaoConfig ledgerDaoConfig;

    private final LedgerDao ledgerDao;

    public DaoSession(Database db, IdentityScopeType type, Map<Class<? extends AbstractDao<?, ?>>, DaoConfig>
            daoConfigMap) {
        super(db);

        ledgerDaoConfig = daoConfigMap.get(LedgerDao.class).clone();
        ledgerDaoConfig.initIdentityScope(type);

        ledgerDao = new LedgerDao(ledgerDaoConfig, this);

        registerDao(Ledger.class, ledgerDao);
    }
    
    public void clear() {
        ledgerDaoConfig.clearIdentityScope();
    }

    public LedgerDao getLedgerDao() {
        return ledgerDao;
    }

}
